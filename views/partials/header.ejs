<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>RakBuku</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link
        href="https://fonts.googleapis.com/css2?family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="./styles/header.css">
    <link rel="stylesheet" href="./styles/style.css">
</head>

<body>
    <div class="navbar">
        <h2><a href="/">RakBuku</a></h2>
        <div class="search-and-profile">
            <input type="text" placeholder="Search Book" class="search-bar" id="search-bar">
            <div class="user-profile">
                <form action="/user" method="post">
                    <div class="user-options hidden">
                        <h3>Users</h2>
                            <% users.forEach(function(user) {%>
                                <button class="user-btn" type="submit" name="user" value="<%= user.user_id %>">
                                    <%= user.user_name %>
                                </button>
                                <% }); %>
                                    <div class="action-btn">
                                        <button type="submit" name="choice" value="new" id="add-user-btn">Add
                                            User</button>
                                        <button type="submit" name="choice" value="del" id="del-user-btn">Delete
                                            User</button>
                                    </div>
                    </div>
                </form>
            </div>


        </div>

        <div class="hamburger">
            <span class="bar"></span>
            <span class="bar"></span>
            <span class="bar"></span>
        </div>

    </div>

</body>

</html>